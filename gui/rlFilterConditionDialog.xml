<?xml version="1.0" encoding="utf-8" standalone="no" ?>
<!--
    rlFilterConditionDialog.xml

    Modal condition editor used by RLMenuSettingsFrame.lua to edit a single
    condition row (field / cmp / value). Mirrors AnimalMoveDestinationDialog
    structurally: fs25_dialogBg + fs25_dialogContentContainer + 3-part bg
    + fs25_dialogButtonBox. Three label+widget rows + OK/Cancel.

    Value widget is a swap pair (TextInput for number fields, MultiTextOption
    for bool fields). RLFilterConditionDialog.lua toggles visibility per the
    active field's catalog type.

    Local profiles `rl_filterDialog_picker` and `rl_filterDialog_textInput`
    pin `anchorTopCenter` so picker and TextInput position relative to the
    content container's top-center, matching the labels (fs25_dialogText is
    also anchorTopCenter). Without these the inherited anchors land widgets
    at the container's bottom-left -> outside the dialog frame.
-->
<GUI onOpen="onOpen" onClose="onClose" onCreate="onCreate">
    <GuiElement profile="newLayer"/>
    <Bitmap profile="dialogFullscreenBg" id="dialogBg"/>
    <GuiElement profile="fs25_dialogBg" id="dialogElement" size="600px 420px">
        <ThreePartBitmap profile="fs25_dialogBgMiddle"/>
        <ThreePartBitmap profile="fs25_dialogBgTop"/>
        <ThreePartBitmap profile="fs25_dialogBgBottom"/>
        <GuiElement profile="fs25_dialogContentContainer">

            <Text profile="fs25_dialogTitle" id="dialogTitleElement"
                  position="0px -30px"
                  text="$l10n_rl_menu_filters_dialog_title"/>

            <!--
                Row 1: Field. Label left, picker right of center.
                Labels reuse existing keys
                (rl_menu_filters_field_label / cmp_label / value_label).
                Explicit size + textAlignment="right" so all
                three labels right-align toward the widget column at x=50px.
                Without an explicit width, textAlignment has no bounding box
                to align within and the change is a no-op.
            -->
            <Text profile="fs25_dialogText" id="fieldLabel"
                  size="160px 28px"
                  textAlignment="right"
                  position="-200px -110px"
                  text="$l10n_rl_menu_filters_field_label"/>
            <MultiTextOption profile="rl_filterDialog_picker" id="fieldPicker"
                             position="50px -110px"
                             onClick="onFieldChanged"
                             focusInit="onOpen">
                <Text profile="fs25_multiTextOptionText" width="240px" name="text"/>
            </MultiTextOption>

            <!--
                Row 2: Comparator. Editable cmps filtered to exclude
                in / notin (multi-value editor deferred).
            -->
            <Text profile="fs25_dialogText" id="cmpLabel"
                  size="160px 28px"
                  textAlignment="right"
                  position="-200px -170px"
                  text="$l10n_rl_menu_filters_cmp_label"/>
            <MultiTextOption profile="rl_filterDialog_picker" id="cmpPicker"
                             position="50px -170px"
                             onClick="onCmpChanged">
                <Text profile="fs25_multiTextOptionText" width="240px" name="text"/>
            </MultiTextOption>

            <!--
                Row 3: Value. TextInput (number) and MultiTextOption (bool)
                occupy the same row position; Lua toggles visibility per
                catalog type at onOpen + onFieldChanged.
            -->
            <Text profile="fs25_dialogText" id="valueLabel"
                  size="160px 28px"
                  textAlignment="right"
                  position="-200px -230px"
                  text="$l10n_rl_menu_filters_value_label"/>
            <!-- Bg + iconBox use local profiles that pin
                 the visible extent to the parent's 280px box so the field
                 lines up with the Field/Compare pickers. See
                 rl_filterDialog_textInputBg / rl_filterDialog_textInputIconBox
                 in <GUIProfiles> below. -->
            <TextInput profile="rl_filterDialog_textInput" id="valueNumberInput"
                       position="50px -230px"
                       focusOnHighlight="true"
                       onEnterPressed="onEnterPressed"
                       onEscPressed="onEscPressed"
                       onTextChanged="onTextChanged"
                       enterWhenClickOutside="false"
                       imeTitle="$l10n_rl_menu_filters_value_label"
                       imeDescription="$l10n_rl_menu_filters_value_label"
                       imePlaceholder="$l10n_rl_menu_filters_value_label">
                <ThreePartBitmap profile="rl_filterDialog_textInputBg"/>
                <Bitmap profile="rl_filterDialog_textInputIconBox">
                    <Bitmap profile="fs25_textInputIcon"/>
                </Bitmap>
            </TextInput>
            <MultiTextOption profile="rl_filterDialog_picker" id="valueBoolPicker"
                             position="50px -230px"
                             onClick="onValueBoolChanged">
                <Text profile="fs25_multiTextOptionText" width="240px" name="text"/>
            </MultiTextOption>

            <!--
                Enum value picker (gender / subType single-pick). Shares
                the value-row slot with the number / bool widgets; Lua toggles
                visibility per catalog type. Reuses the same rl_filterDialog_picker
                profile as the field/cmp pickers (no new profile per spec).
            -->
            <MultiTextOption profile="rl_filterDialog_picker" id="valueEnumPicker"
                             position="50px -230px"
                             onClick="onValueEnumChanged">
                <Text profile="fs25_multiTextOptionText" width="240px" name="text"/>
            </MultiTextOption>

            <!--
                String value input (name contains/notcontains). Shares
                the value-row slot. Reuses the rl_filterDialog_textInput profile
                from the number input (length cap 30 already declared on the
                profile). Empty-string commit is rejected at OK click and
                surfaces hintText.
            -->
            <!-- Same bg + iconBox local profile usage as
                 valueNumberInput above. -->
            <TextInput profile="rl_filterDialog_textInput" id="valueStringInput"
                       position="50px -230px"
                       focusOnHighlight="true"
                       onEnterPressed="onEnterPressed"
                       onEscPressed="onEscPressed"
                       onTextChanged="onTextChanged"
                       enterWhenClickOutside="false"
                       imeTitle="$l10n_rl_menu_filters_value_label"
                       imeDescription="$l10n_rl_menu_filters_value_label"
                       imePlaceholder="$l10n_rl_menu_filters_value_label">
                <ThreePartBitmap profile="rl_filterDialog_textInputBg"/>
                <Bitmap profile="rl_filterDialog_textInputIconBox">
                    <Bitmap profile="fs25_textInputIcon"/>
                </Bitmap>
            </TextInput>

            <!--
                Summary button shown in the value-row slot when cmp =
                `in`/`notin`. Opens RLFilterValueSetDialog for multi-select.
                Lua sets text to "N selected" via refreshValueSetSummary; size
                matches the 280x36 value-row slot so the swap visually replaces
                the picker/text input without layout shift.
            -->
            <Button profile="rl_filterDialog_summaryButton" id="valueSetButton"
                    position="50px -230px"
                    onClick="onClickOpenValueSet"
                    visible="false"
                    text="">
                <ThreePartBitmap profile="fs25_settingsButtonBg"/>
            </Button>

            <!--
                Inline hint surface for reject paths (empty-string,
                subType-domain-empty refuse). Hidden by default; populated via
                RLFilterConditionDialog:showHint(l10nKey). Also unblocks the
                 deferred silent-numeric-reject hint.
            -->
            <Text profile="fs25_dialogText" id="hintText"
                  position="0px -290px"
                  visible="false"
                  text=""/>

        </GuiElement>

        <BoxLayout profile="fs25_dialogButtonBox" id="buttonsPC">
            <Button profile="buttonOK" text="$l10n_button_ok"
                    onClick="onClickOk" id="okButton"/>
            <Bitmap profile="fs25_dialogButtonBoxSeparator"/>
            <Button profile="buttonBack" text="$l10n_button_back"
                    onClick="onClickBack" id="backButton"/>
        </BoxLayout>
    </GuiElement>

    <!--
        Local dialog profiles. Element-level `with=` is silently ignored
        by the GUI loader (see project memory reference_fs25_gui_gotchas)
        - only profile-level `with=` traits are honored, so we have to
        declare these here rather than override on the elements above.
    -->
    <GUIProfiles>
        <Profile name="rl_filterDialog_picker" extends="fs25_multiTextOption" with="anchorTopCenter">
            <size value="280px 36px"/>
        </Profile>
        <Profile name="rl_filterDialog_textInput" extends="fs25_textInput" with="anchorTopCenter">
            <size value="280px 36px"/>
            <textBold value="true"/>
            <maxCharacters value="30"/>
            <maxInputTextWidth value="250px"/>
        </Profile>
        <!--
            The inherited bg renders wider than the parent
            TextInput's 280px box, so the value field looks visibly wider
            than the Field/Compare pickers. Reset absoluteSizeOffset to 0
            so the bg fills exactly the parent box and matches the picker
            visible extent.
        -->
        <Profile name="rl_filterDialog_textInputBg" extends="fs25_textInputBg">
            <absoluteSizeOffset value="0px 0px"/>
        </Profile>
        <!--
            The inherited iconBox renders the pencil
            outside the parent TextInput's right edge, adding visual width
            past the picker outline. Override position so the iconBox sits
            flush inside the parent's right edge instead, keeping the
            pencil affordance + focus rendering but matching the picker
            visible extent.
        -->
        <Profile name="rl_filterDialog_textInputIconBox" extends="fs25_textInputIconBox">
            <position value="0px 0px"/>
        </Profile>
        <!--
            Summary button at the value-row slot. Mirrors the RLRM
            General-tab settings-action button pattern (our mod's
            rl_settingsButton profile + the fs25_settingsButtonBg child for
            the bordered bitmap background); diff from that pattern is only
            in anchor (anchorTopCenter so position="50px -230px" resolves
            against the dialog content container's top-center) + size
            (280x36 to match the value-row slot widgets). No inputAction;
            click opens the value-set dialog. focusOnHighlight=true makes
            the button keyboard/gamepad-navigable.
        -->
        <Profile name="rl_filterDialog_summaryButton" extends="emptyPanel" with="anchorTopCenter">
            <size value="280px 36px"/>
            <focusOnHighlight value="true"/>
            <textResizeWidth value="200px"/>
            <textSize value="16px"/>
            <iconColor value="$preset_colorTransparent"/>
            <iconFocusedColor value="$preset_colorTransparent"/>
            <iconTextOffset value="0px -1px"/>
            <iconHighlightedColor value="$preset_colorTransparent"/>
            <textBold value="true"/>
            <textUpperCase value="true"/>
            <textAlignment value="center"/>
            <textColor value="$preset_fs25_colorMainLight"/>
            <textFocusedColor value="$preset_fs25_colorMainDark"/>
            <textDisabledColor value="$preset_colorDisabled"/>
        </Profile>
    </GUIProfiles>
</GUI>
